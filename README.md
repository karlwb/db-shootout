# Start up databases in docker swarm

```
docker stack deploy db -c ./docker-compose.yml
```

# install deps if needed
```poetry install```

# start poetry shell
```poetry shell```

# run test
```poe test```



